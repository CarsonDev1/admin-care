import ConfigViewPage from '@/app/dashboard/config/_components/ConfigViewPage';
import React from 'react';

export const metadata = {
  title: 'Dashboard : Config view'
};

const page = () => {
  return (
    <div>
      <ConfigViewPage />
    </div>
  );
};

export default page;
